# Core dependency for document conversion
pypandoc>=1.13

# PDF engine for converting to PDF without system dependencies
weasyprint>=60.0

# pypandoc will automatically download and manage Pandoc binary
# No system installation of Pandoc required!
